python:
  - "2.7"
# command to install dependencies
install: "sudo apt-get install pylint && pip install pygithub"
# command to run tests
script: pylint clonegits.py --disable=import-error \
            --disable=fixme --disable=bad-continuation
