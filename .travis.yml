python:
  - "2.7"
# command to install dependencies
install: "sudo apt-get install pylint; sudo pip install pygithub"
# command to run tests
script: pylint clonegits.py --disable=fixme --disable=bad-continuation
